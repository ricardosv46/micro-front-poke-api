import type { NavItem } from "../../../core/layout/types";
import { HISTORY_ROUTES } from "./paths";

export const historyNavItem: NavItem = {
  label: "Historial",
  path: HISTORY_ROUTES.HISTORY,
};
